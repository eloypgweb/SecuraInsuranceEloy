import { FormatoDineroPipe } from './formato-dinero.pipe';

describe('FormatoDineroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDineroPipe();
    expect(pipe).toBeTruthy();
  });
});
